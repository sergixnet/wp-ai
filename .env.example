# WordPress AI - Entorno de Desarrollo

## Configuración Inicial Rápida

```bash
# 1. Copiar este archivo a .env
cp .env.example .env

# 2. Generar certificados SSL
mkdir -p nginx/ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout nginx/ssl/key.pem \
  -out nginx/ssl/cert.pem \
  -subj "/C=ES/ST=State/L=City/O=Organization/CN=wp-ai.dev"

# 3. Agregar dominio al hosts
# Linux/Mac:
echo "127.0.0.1 wp-ai.dev" | sudo tee -a /etc/hosts
# Windows (PowerShell como Admin):
# Add-Content -Path C:\Windows\System32\drivers\etc\hosts -Value "`n127.0.0.1 wp-ai.dev"

# 4. Iniciar contenedores
docker compose up -d

# 5. Acceder a https://wp-ai.dev
```

## Variables de Entorno

Configura estas variables según tus necesidades:

```env
# MySQL Configuration
MYSQL_ROOT_PASSWORD=root_secure_password_2024
MYSQL_DATABASE=wordpress_db
MYSQL_USER=wordpress_user
MYSQL_PASSWORD=wordpress_secure_password_2024
```

## Notas de Seguridad

⚠️ **IMPORTANTE**: Cambia las contraseñas antes de usar en producción.

Este archivo es solo una plantilla. El archivo `.env` real no debe subirse a Git.
