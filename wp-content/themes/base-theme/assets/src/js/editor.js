/**
 * Block Editor JavaScript
 * 
 * Custom scripts for the WordPress block editor (Gutenberg).
 * 
 * @package Base_Theme
 */

// Block editor specific functionality
document.addEventListener('DOMContentLoaded', () => {
  console.log('Block editor scripts loaded');
});
